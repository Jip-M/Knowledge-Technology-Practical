# Knowledge-Technology-Practical

To run the prototype, open your favourite virtual environment, and download the requirements in requirements.txt.

Then type in terminal "streamlit run main.py". The process will open up a streamlit browser.

